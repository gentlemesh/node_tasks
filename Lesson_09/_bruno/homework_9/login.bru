meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: none
}

body:json {
  {
    "email": "test@test.local",
    "password": "123456"
  }
}

script:post-response {
  bru.setVar('token', res.body.token);
}
