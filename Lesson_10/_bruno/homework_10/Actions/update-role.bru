meta {
  name: update-role
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/update-role
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "userId": 1,
    "newRole": "admin"
  }
}
