meta {
  name: loginAsUser
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: none
}

body:json {
  {
    "username": "user",
    "password": "111"
  }
}

script:post-response {
  bru.setVar('token', res.body.token);
}
